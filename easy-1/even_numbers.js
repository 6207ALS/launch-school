/*
The following code logs all even numbers from 1 to 99, inclusive, to the 
console. It simply uses a for loop that increments by values of 2 (starting from
the value 2) to log every even number.  
*/

for (let i = 2; i < 99; i+=2) {
    console.log(i);
}