let person = {
  name:       'Bob',
  occupation: 'web developer',
  hobbies:    'painting',
};

/*
There are two ways to access the name of the person object: use bracket 
notation with the key's name inside or use dot notation followed by the
key's name.
*/

console.log(person['name']);
console.log(person.name);