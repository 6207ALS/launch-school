let foo = ['a', 'b', 'c'];
console.log(foo.length);
console.log(foo[3]);

// The last line of code would throw an error because it is attempting
// to access an index outside of the foo array's bounds. "foo" only
// has three elements, whereas the expression attempts to access the 
// fourth element.