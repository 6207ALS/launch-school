console.log('12' < '9');

// The expression above appears to evaluate to true. As explained,
// Javascript will compare character-by-character from left to right
// when comparing strings (not numbers).It evaluates '1' is less than
// '9', so the result is false. 