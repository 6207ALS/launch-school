/*
The following code logs all odd numbers from 1 to 99, inclusive, to the console.
It uses a for loop to iterate over every number, and determines if the number
is even or odd. 
*/

for (let i = 1; i <= 99; i++) {
    if (i % 2 === 1) console.log(i);
}