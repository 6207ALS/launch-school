// The following will not log anything to the console. Nothing within the
// scream function tells it to log to the console. It will, however, return
// a modified string value. 

function scream(words) {
    return words + '!!!!';
}
  
scream('Yipeee');